Unfortunately I made a mistake with documenting the progress of my project. I have made a habit of self-documenting when working on personal projects, and that habit remained with my work on this project. However, the full self documentation process is in the repository, and the development of the implementation can be somewhat followed. Furthermore, please also find attached a pdf, "notes.pdf" which contains time-stamped documentated notes for what I had researched each week. Here, I will also make an attempt to explain the direction that the implementation took, since I have committed to the repository so infrequently. The first folder, PKgen, contains the development of the key generation, encryption and decryption. It ends with "PKgen6.py", which is a fully functioning "correct" scheme. It also contains a "test.py" file which is where I experimented with np.arrays. The scheme folder, is where I sepereated the key generation, encryption and decryption into seperate files. The file "main.py", esentially incorporates everything, into a program that allows the entry of a bit-string and returns the decrypted bit-string once it has gone through the process of encrpytion. Next, comes network where I first implemented the relay-server. Encryption does not work here. "complete" is our final folder of the development process. In client B, we can see interations of the client: "client.py" and "client2.py", which is a rough documentation of major changes to the client file. The same can be seen in the server folder with: "server.py", "server2.py" and "server3.py". The final version of the project, is "finalmessaging" where I have removed unused files created during development, and also cleaned up the parameters to be more inline with more production level schemes.